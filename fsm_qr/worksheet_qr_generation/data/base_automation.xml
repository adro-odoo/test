<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <record id="action_create_attachment_and_qr" model="ir.actions.server">
        <field name="name">Create Attachment and QR</field>
        <field name="model_id" ref="web_studio.model_studio_approval_entry"/>
        <field name="state">code</field>
        <field name="code">
for record in records:
    method = record.rule_id.method
    if record.model == 'project.task' and method == 'action_fsm_validate':
        task_id = record.res_id
        task = env['project.task'].search([('id' , '=', task_id)])
        if task:
            ir_actions_report_sudo = env['ir.actions.report'].sudo()
            worksheet_report_action = env.ref('worksheet_qr_generation.action_report_worksheet_template')
            worksheet_report = worksheet_report_action.sudo()
            content, _content_type = ir_actions_report_sudo._render_qweb_pdf(worksheet_report, res_ids=[task.id])
            attachment = env['ir.attachment'].create({
                'name': f"{task.name} Worksheet Template.pdf" if record.name else "Worksheet Template.pdf",
                'type': 'binary',
                'mimetype': 'application/pdf',
                'raw': content,
                'res_model': 'project.task',
                'res_id': task.id,
                'public': True,
            })
        </field>
    </record>

    <record id="base_automation_action_set_approval_confirming_user" model="base.automation">
       <field name="name">Set Approval Confirming User</field>
        <field name="model_id" ref="web_studio.model_studio_approval_entry"/>
        <field name="trigger">on_create_or_write</field>
        <!-- <field name="filter_pre_domain">[("state", "!=", "purchase")]</field>
        <field name="filter_domain">[("state", "=", "purchase")]</field> -->
        <field name="action_server_ids" eval="[Command.link(ref('worksheet_qr_generation.action_create_attachment_and_qr'))]" />
    </record>
</odoo>
